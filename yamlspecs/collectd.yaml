!include pymodule.yaml
---
- package: collectd
  root: "{{purepyroot}}"
  name: collectd
  version: "{{versions.collectd}}"
  vendor_source:  https://github.com/downloads/appliedsec/{{name}}/{{name}}-{{version}}.{{extension}}
  description: |
     Python implementation of the binary protocol used by the collectd Network plugin.
     Collectd let you send arbitrary numeric data to collectd servers.
     {{description_src}}
